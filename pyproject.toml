[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
# Conservative set: pyflakes (F), pycodestyle error subsets (E4/E7/E9), import sorting (I).
# No opinionated style rules (no E501 line-length, no D/docstring, no ANN, no COM, etc.)
select = ["E4", "E7", "E9", "F", "I"]
